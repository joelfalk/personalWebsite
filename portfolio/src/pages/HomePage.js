import React from 'react';
import Hero from '../components/Hero';
import Carousel from "../components/Carousel";

function HomePage(props) {

    return (
        <div id={props.id}>Home</div>
    );

}

export default HomePage;